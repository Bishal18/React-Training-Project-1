const config = {
    apiSettings : {
        baseURl: 'https://reactprojectdbserver.azurewebsites.net/',
        productRoute: "/products",
        categoryRoute: "/categories",
        orderRoute: "/orders",
        usersRoute: "/users" 
    }
}